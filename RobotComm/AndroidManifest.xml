<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.denbar.RobotComm"
    android:versionCode="1"
    android:versionName="@string/version" >

    <uses-sdk android:minSdkVersion="8" />

    <application
        android:icon="@drawable/icon9thsense"
        android:label="@string/app_name" >
        <activity
            android:label="@string/app_name"
            android:name=".RobotCommActivity" >
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:label="RobotComm Credentials"
            android:name=".credentialsActivity" />
        <activity
            android:label="RobotComm Help"
            android:name=".Help" />

        <service
            android:enabled="true"
            android:label="RobotCommService"
            android:name=".RobotCommService" >
            <intent-filter >
                <action android:name="com.denbar.RobotComm.RobotCommService" />
            </intent-filter>
        </service>

        <!--
        callback for boot completed, note that you need the permission here as well
        to know when boot is finished
        -->
        <receiver
            android:enabled="true"
            android:name=".StartupIntentReceiver" >
            <intent-filter >
                <action android:name="android.intent.action.BOOT_COMPLETED" />

                <category android:name="android.intent.category.HOME" />
            </intent-filter>
        </receiver>
        <receiver
            android:icon="@drawable/icon9thsense"
            android:label="Robot Status"
            android:name=".RobotCommWidget" >
            <intent-filter >
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info" />
        </receiver>

        <service android:name=".UpdateWidgetService" >
        </service>

        <activity
            android:label="C2DM ReceiverActivity"
            android:name=".C2DM_ReceiverActivity" >
        </activity>

        <!-- callback for C2DM registration successful (gets the registration ID) -->
        <receiver android:name=".C2DMRegistrationReceiver" >
            <intent-filter >
                <action android:name="com.google.android.c2dm.intent.REGISTRATION" >
                </action>

                <category android:name="com.denbar.RobotComm" />
            </intent-filter>
        </receiver>
        <receiver android:name=".C2DMMessageReceiver" >
            <intent-filter >
                <action android:name="com.google.android.c2dm.intent.RECEIVE" >
                </action>

                <category android:name="com.denbar.RobotComm" />
            </intent-filter>
        </receiver>

        <!-- callback for RETRY on the C2DM registration -->
        <receiver android:name=".C2DMRegistrationReceiver" >
            <intent-filter >
                <action android:name="com.google.android.c2dm.intent.RETRY" />

                <category android:name="com.denbar.RobotComm" />
            </intent-filter>
        </receiver>

        <!-- activities -->
        <activity android:name="RegistrationResultActivity" >
        </activity>
        <activity android:name="MessageReceivedActivity" >
        </activity>

        <!-- startup service that does the registration -->
        <service android:name=".C2DMRegistrationService" >
            <intent-filter >
                <action android:name="com.denbar.RobotComm.C2DMRegistrationService" />
            </intent-filter>
        </service>
    </application>

    <uses-permission android:name="com.denbar.RobotComm.permission.C2D_MESSAGE" />
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />
    <uses-permission android:name="com.google.android.c2dm.permission.SEND" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"></uses-permission>
	<uses-permission android:name="android.permission.UPDATE_DEVICE_STATS"></uses-permission>
	<uses-permission android:name="android.permission.CHANGE_WIFI_STATE"></uses-permission>
	<uses-permission android:name="android.permission.WAKE_LOCK"></uses-permission>
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <!-- permission for C2DM API -->
    <permission
        android:name="com.denbar.RobotComm.permission.C2D_MESSAGE"
        android:protectionLevel="signature" />

</manifest>